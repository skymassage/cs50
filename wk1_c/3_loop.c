#include <stdio.h>
#include <stdbool.h> // use boolean algebra
#include <cs50.h>

int main(void)
{
    /*
    int i = 0;
    while (i < 3)
    {
        printf("meow\n");
        i++;
    }
    */

    /* this loop, unlike a while loop, is guaranteed to run at least one time.
    int n;
    do
    {
        n = get_int("Size: ");
    }
    while (n < 100);
    */

    /*
    for (int i = 0; i < 3; i++)  // "i++" and "++i" are the same in the "for" loop.
    {
        printf("meow\n");
    }
    */

    /*   a infinite loop
    while (1) // while (true)
    {
        printf("meow\n");
    }
    */
}